package app.verse.prototype.plur;

import androidx.annotation.NonNull;
import android.app.Activity;

/**
 * Simplified NostrmoPlugin implementation to allow successful builds.
 * This is a placeholder that will be replaced with the actual implementation
 * once the basic build structure is working.
 */
public class NostrmoPlugin {
    private final Activity activity;
    
    public NostrmoPlugin(Activity activity) {
        this.activity = activity;
    }
}