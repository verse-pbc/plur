package app.verse.prototype.plur

import android.app.Activity
import android.os.Bundle

/**
 * Custom FlutterActivity implementation to allow successful builds.
 * This is a placeholder that will be replaced with the actual Flutter implementation
 * once the basic build structure is working.
 */
open class FlutterActivity: Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}