name: sentry_flutter
description: A stub package for sentry_flutter to avoid dependencies
version: 0.0.0
homepage: https://example.com

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter: